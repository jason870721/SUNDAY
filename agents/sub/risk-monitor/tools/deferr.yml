# Deferred — the framework advertises these names and auto-adds tool_search (RP-19).
- repl        # python scenario tables: "if BTC −10%, account drawdown = ?"
- json_query  # pluck fields from large /api/account/pnl responses
