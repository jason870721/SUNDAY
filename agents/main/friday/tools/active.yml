# Regular evva tools only. Swarm tools (send_message, list_members, task_*,
# schedule_set, schedule_clear) are injected automatically by the leader role —
# do NOT list them here (listing them double-registers and breaks the run).
- bash          # operate Sunday over curl (the only way agents touch Sunday)
- web_fetch     # pull external context when judging the market
