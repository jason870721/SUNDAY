# Deferred — advertised in the system prompt's 工具櫃 (the harness doesn't list these).
- json_query  # pluck fields from large trades/orders responses
- excel       # optional offline performance workbook (docs/ledger.xlsx) if the User asks
