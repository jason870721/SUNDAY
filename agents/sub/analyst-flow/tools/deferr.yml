# Deferred — the framework advertises these names and auto-adds tool_search (RP-19).
- repl        # python: cross-symbol correlation, volatility stats, quick signal sanity checks
- json_query  # pluck fields from large klines/markets responses
