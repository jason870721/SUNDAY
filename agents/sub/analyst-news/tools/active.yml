# Swarm tools (send_message, my_tasks, task_get) are injected by the worker role.
- web_search    # news / events / narrative — this analyst's home turf
- web_fetch
- http_request  # read Sunday (/desk, /status) + POST /commentary
- skill
