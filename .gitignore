# secrets / env — Binance testnet keys, NEVER commit
.env
.env.*
*.env
!.env.example

# evva swarm runtime state (created by `evva swarm .`)
.vero/

# watchdog agent's last-Top10 snapshot (runtime comparison state)
.watchdog-markets.json

# scheduled-alarm queue written by the evva swarm runtime (not Sunday product state)
alarms.json

# Sunday runtime SQLite — created on boot (alerts / memory / reports / kv); never commit
*.db
*.db-shm
*.db-wal

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# logs / data dumps
*.log
log/
logs/

# OS / editor
.DS_Store
.idea/
.vscode/
