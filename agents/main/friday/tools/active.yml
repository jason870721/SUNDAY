# Regular evva tools. Swarm tools (task_create/assign/verify, send_message,
# list_members, schedule_set/clear) are injected automatically by the leader
# role — do NOT list them here.
- bash # operate Sunday via curl (see the operate-sunday skill / curl :7777/manual)
- web_fetch
