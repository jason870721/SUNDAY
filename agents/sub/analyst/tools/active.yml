# Regular evva tools only. Swarm tools (send_message, list_members, my_tasks,
# task_get) are injected automatically by the worker role — do NOT list them here.
- bash          # query Sunday over curl (read-only; no levers)
- web_fetch     # external market context (news / sentiment)
- web_search
